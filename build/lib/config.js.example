// rename this to config.js
const ha = {
  internalUrl = "http://homeassistant.local:8123";  // internal
  externalUrl = "http://your_external_ip:8123";         // external
  token = "your super secret api access token";
};

const timeoutInterval = {
  internal: 6, // Timeout for internal URL
  external: 3, // Timeout for external URL
};

const timestampOffset = {
  days: 0,
  hours: 2,
  minutes: 0,
  seconds: 0
}

module.exports = { ha, timeoutInterval, timestampOffset }
